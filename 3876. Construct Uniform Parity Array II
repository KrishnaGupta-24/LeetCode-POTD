bool uniformArray(vector<int>& nums) {
        int xmin=nums[0];
        bool odd=0;
        for(auto &x:nums){
            xmin=min(xmin,x);
            odd|=x&1;
        }
        return (xmin&1)==odd;
    }
